[project]
name = "habic-tracker"
version = "0.1.0"
description = "simple trackeador de habitos de prueba"
readme = "README.md"
authors = [
    { name = "AngelPortilla1", email = "Angelportihernan019@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
habic-tracker = "habic_tracker:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
